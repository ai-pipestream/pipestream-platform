# Test configuration - disable actual registration during tests
pipestream.registration.enabled=false
quarkus.application.name=test-service
quarkus.application.version=1.0.0-test

# PostgreSQL DevServices
quarkus.datasource.devservices.image-name=postgres:18
