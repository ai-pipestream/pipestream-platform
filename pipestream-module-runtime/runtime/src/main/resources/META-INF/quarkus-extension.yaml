name: "Pipestream Module Runtime"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
description: "Demand-pull worker framework (ModuleWorkerLoop) for Pipestream modules"
metadata:
  keywords:
    - "pipestream"
    - "module"
    - "worker"
    - "grpc"
  categories:
    - "messaging"
  status: "stable"
