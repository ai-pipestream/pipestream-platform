# Use random ports for tests
%test.quarkus.http.test-port=0
%test.quarkus.grpc.server.test-port=0

# Default URL for REST client — DevService overrides this at runtime
quarkus.rest-client.djl-serving.url=http://localhost:8080
