# JUnit 5 parallel execution configuration
# Disable JUnit 5's internal parallel execution - Gradle handles parallelism via maxParallelForks
# This prevents double-parallelism and ensures each test fork runs tests sequentially
junit.jupiter.execution.parallel.enabled=false
